#include <stdio.h>
int main()
{
    char a;
    scanf("%d",&a);
    if(ch >= 'A' && ch <= 'Z')
    {
        printf("Uppercase");
    }
    else
    {
        printf("Lowercase");
    }
}